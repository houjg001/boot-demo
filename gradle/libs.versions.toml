[versions]
spring-boot-version = "4.1.1"
spring-dependency-management = "1.1.7"
mybatis-version = "4.1.0"
mapstruct-version = '1.6.3'

[plugins]
springBoot = { id = 'org.springframework.boot', version.ref = "spring-boot-version" }
springDependencyManagement = { id = 'io.spring.dependency-management', version.ref = "spring-dependency-management" }

[libraries]
mybatis = { group = "org.mybatis.spring.boot", name = "mybatis-spring-boot-starter", version.ref = 'mybatis-version' }
pagehelper = { group = 'com.github.pagehelper', name = 'pagehelper-spring-boot-starter', version = '4.1.1' }
mapStruct = { group = 'org.mapstruct', name = 'mapstruct', version.ref = 'mapstruct-version' }
mapStructProcessor = {group = 'org.mapstruct', name = 'mapstruct-processor', version.ref='mapstruct-version'}